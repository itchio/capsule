
stages:
  - build

compile:windows:
  stage: build
  tags:
    - msbuild
  script:
    - scripts/ci-compile.rb windows
  artifacts:
    paths:
      - compile-artifacts

compile:darwin:
  stage: build
  tags:
    - darwin
  script:
    - scripts/ci-compile.rb darwin
  artifacts:
    paths:
      - compile-artifacts

      
